using System;
using System.Collections.Generic;
using System.Threading;
namespace CsharpPractice
{
    class HW1_1_2
    {/*
        static void Main(string[] args)
        {
            // 원의 반지름을 입력받음
            double radius = double.Parse(Console.ReadLine());

            // 실수 자료형(double)의 변수 PI 선언 및 할당
            double PI = 3.14;

            // 각 둘레와 넓이를 int 형으로 변환 후 출력.
            Console.WriteLine("둘레 : " + (int)( 2 * PI * radius) + " 넓이 " + (int)(PI * radius * radius));
        }
        */
    }
}